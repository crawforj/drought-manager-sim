__pycache__/
*.pyc
state/cache/
.pytest_cache/
*.egg-info/
# Internal debug artifact from synth/generate_panel.py -- carries the
# ground-truth tier assignment, which the shipped dataset deliberately
# doesn't expose (see README: validation uses the usage-volume proxy
# method, the same as anyone cloning this repo). Not sensitive (all
# synthetic), just out of scope for what's meant to be "the dataset."
synthetic_accounts_debug.parquet
